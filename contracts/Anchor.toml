[features]
seeds = false
skip-lint = false

[programs.localnet]
clob_dex = "BeD2ptKNis326cLsk1SNT9KamDdDdM69xVk9UrStTY74"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
client = "yarn run ts-node client/*.ts"
